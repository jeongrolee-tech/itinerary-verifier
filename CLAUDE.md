# 작업 방식

이 저장소에서 Claude Code와 일하는 규칙이다.

## 역할 구분

- **설계와 판단은 내가 한다.** 무엇을 검증할지, 어떤 출처를 신뢰할지,
  라벨을 어떻게 매길지는 내가 정한다. 대신 결정해 주지 말 것.
- 구현에는 Claude를 활용하되, **생성된 코드의 이해/검토/검증 및 최종 반영은 내가 책임진다.**
- **터미널 명령은 실행 전에 승인을 받는다.** 조회·테스트처럼 **되돌릴 수 있는** 명령은
  바로 돌려도 된다. 되돌리기 어려운 것은 명령을 먼저 보여주고 내가 승인한다 —
  `git push` · `git reset --hard` · 파일 삭제 · **외부 API 호출**(과금이 붙고 약관 제약을 받는다).
  실행 결과를 해석하고 다음을 정하는 것은 내 몫이다. 명령이 성공한 것과 결과가 맞는 것은 다르다.

## 설명 수준

- 코드나 명령을 낼 때는 **그게 무엇을 하는지, 그렇게 안 하면 무엇이 깨지는지**를 같이 적는다.
- 한 번에 하나씩 간다. 여러 변경을 묶어서 한꺼번에 내지 않는다.

## 이 프로젝트에서 특히 지킬 것

- **근거 없이 값을 만들어 넣지 않는다.** 모르면 모른다고 표시한다.
  판정 코어가 `unknown`을 내는 규칙과 같은 원칙을 작업에도 적용한다.
- 코드에 버그가 있을 수도 있기 때문에, **코드와 문서가 다르면 어느 쪽이 맞다고 먼저 가정하지 않는다.**
  요구사항 → 공식 출처/원본 데이터 → 테스트 → 코드 → 문서를 다시 확인해서 틀린 쪽을 수정한다.
- **라벨(`core/tests.json`)은 판정 코드를 보고 고치지 않는다.** 순환 논리가 된다.
  데이터 스냅샷이 바뀌어 라벨이 노후화됐다면, 코드 출력이 아니라 공식 출처를 다시 보고 정한다.
